{
    "project_name": "Angular Ninja",
    "project_slug": "{{ cookiecutter.project_name|lower|replace(' ', '-') }}",
    "angular_slug": "app-{{ cookiecutter.project_slug }}",
    "api_slug": "api-{{ cookiecutter.project_slug }}",
    "python_slug": "{{ cookiecutter.api_slug|replace('-', '_') }}",
    "project_description": "This is a basic Angular with Django implementation that also supports celery with kubernetes.",
    "postgres_hostname": "postgres-{{ cookiecutter.project_slug }}",
    "postgres_volume": "{{ cookiecutter.redis_hostname }}-data"
}
